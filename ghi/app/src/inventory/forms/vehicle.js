import React from 'react';
export default class VehicleModelForm extends React.Component {
    // > non-functional -> drafted outlined only
        // constructor -> define props

        // handle Submit

        // handle property changes (with submit)

        // mount component -> ref data models

        // render return
    render() {
        return(
            <>
                <h3 className="card-title">NEW CAR MODEL FORM PLACEHOLDER</h3>
            </>
        )
    }
}